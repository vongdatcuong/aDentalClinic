import figures from './figures';
import strings from './strings';
import keys from "./keys";
const configs  = {
    keys: keys,
    figures: figures,
    strings: strings    
}

export default configs;