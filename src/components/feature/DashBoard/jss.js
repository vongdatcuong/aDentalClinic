import {
    drawerLeftWidth,
    transition,
    boxShadow,
    primaryColor,
    primaryBoxShadow,
    infoColor,
    successColor,
    warningColor,
    dangerColor,
    fontColor,
    whiteColor,
    grayColor,
    blackColor,
    hoverBrightColor,
    hexToRgb
  } from "../../../themes/theme1";
  
const dashboardStyle = (theme) => ({
    container: {
        flexGrow: 1
    }    
});
  
export default dashboardStyle;  